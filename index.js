//import { getDatabase, ref, set } from "https://www.gstatic.com/firebasejs/9.14.0/firebase-database.js";
//const db = getDatabase();
function laheta() {
    window.alert("so close");
   // var teksti = document.getElementById("textikenttä");
    //var läheta = document.getElementById("lähetä");
    //set(ref(db), {
       // viesti: "sometekshe"
   // });
}

export { laheta };